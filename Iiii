#include <stdio.h>

int main() {
    int v1, v2, v3, v4, v5;  // ← добавить v4 и v5
    int max;
    
    printf("Введите 5 чисел: ");
    scanf("%d%d%d%d%d", &v1, &v2, &v3, &v4, &v5);  // ← 5 штук %d
    
    max = v1;
    if (v2 > max) max = v2;
    if (v3 > max) max = v3;
    if (v4 > max) max = v4;
    if (v5 > max) max = v5;
    
    printf("max = %d\n", max);
    return 0;
} 
